set -e

pnpm exec lint-staged

doctor_output="$(NO_COLOR=1 pnpm exec react-doctor)"
printf '%s\n' "$doctor_output"

if ! printf '%s\n' "$doctor_output" | grep -Eq '100[[:space:]]*/[[:space:]]*100'; then
  printf '%s\n' "React Doctor score must be 100/100."
  exit 1
fi
